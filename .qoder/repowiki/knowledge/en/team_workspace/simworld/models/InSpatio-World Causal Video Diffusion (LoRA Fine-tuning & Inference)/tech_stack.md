PyTorch + `diffusers` (`ModelMixin`/`ConfigMixin`) for the Wan DiT backbone; `accelerate` (DDP) for distributed training; `peft` (`LoraConfig`, `inject_adapter_in_model`) for LoRA injection; `omegaconf` for YAML config with dotted-key CLI overrides; `decord` bridge to torch tensors for video I/O; safetensors for checkpoint IO; optional SAGE Attention / FlashAttention-2/3 and `torch.compile` (inductor) for inference acceleration.