Install as editable MMEditing clone: `pip install -e .`. Training runs via `python tools/train.py configs/clipiqa/clipiqa_coop_koniq.py`; distributed variants use `tools/dist_train.sh` / `tools/slurm_train.sh`. Demo scripts in `demo/` accept `--config` and `--checkpoint` flags. CLIP checkpoints are resolved from `/workspace/group_share/adc-sim/users/cloudsim/clip` before downloading to `~/.cache/clip`.