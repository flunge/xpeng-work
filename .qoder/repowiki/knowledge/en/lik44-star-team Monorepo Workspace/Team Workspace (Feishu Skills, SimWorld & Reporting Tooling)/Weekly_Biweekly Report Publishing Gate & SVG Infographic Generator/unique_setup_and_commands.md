`extract_font.py` must be run once to produce `team/scripts/fonts/NotoSansCJKsc-Bold.ttf` from `/usr/share/fonts/opentype/noto/NotoSansCJK-Bold.ttc`; `html2svg.mjs` depends on `npm install satori satori-html`. The canonical publish gate is `python3 team/scripts/preflight.py <doc_token> [--audience xianming|boss] [--topics t1,t2,...]`, which internally invokes `check_report.py` and `gen_svg_infographic.py` per topic.