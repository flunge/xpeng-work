Requires host tools installed and available on PATH: `ossutil64`, `lz4`, `ffmpeg` (with hevc_nvenc GPU support), and `camera_video_xp5_decoder` (override path via env `CAMERA_VIDEO_DECODER`). `dds_file_utils.py` also expects `pipeline/ucp/download_file_from_oss2` to be reachable by inserting its parent repo root into `sys.path`. `create_multi_vehicle_scenario.py` additionally needs `ucp.tools.ucp_producer.UcpStreamingMsgProducer` and Kafka connectivity to topics `cloudsim_3dgs[_dev]`.