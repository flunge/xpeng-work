<title>WEB配置参数含义说明</title>

# WEB配置参数含义说明

> 来源: https://yinerda.yuque.com/yt1fh6/4gdtu/gsccog81mv0hpii7  
> 路径: Web配置工具 > 参数设置 > WEB配置参数含义说明

根据不同的产品型号，支持的功能不通，UI看到的界面有差异，以实际设备看到UI为准。比如YED-RY1440有输入输出，就能看到GPIO界面。YED-Core-Air724没有GPIO输入输出，就看不到GPIP界面。

不同的固件版本，对UI显示的功能支持情况不一样，如果配置参数后无效，升级最新DTU固件。

随着固件功能功能的升级，UI可能变化。

# 一、基本参数

|  |  |  |
|-|-|-|
| 参数项目 | 参数说明 | 备注 |
| 网络分帧时间 | 网络数据分包超时时间，当2个字节的数据间隔大于这个时间后，将发送给串口 |  |
| 设置参数密码 | 如果设置了密码，当下一次设置的时候必须输入同样的密码才能设置，否者只能本地复位模组参数，才能更新新参数(注意当密码错误后，服务器显示参数更新，但是设备实际更新不成功)。 同时本地串口必须验证密码后才能读取参数，可以防止非法访问设备的参数。 |  |
| DTU日志输出 | 控制DTU调试日志。日志会有设备信息，可以用于调试设备 |  |
| DTU固件更新 | 控制DTU固件升级功能。可以升级成最新的DTU固件。当然固件是稳定的，就不建议随便升级。 |  |
| 分组参数更新 | 自动更新表示设备在不重启的情况下，24小时后会自动请求一次。 手动更新表示设备只会更新第一次参数，后面不会主动更新参数，除非设备恢复出厂设置。 |  |
| DTU自动重启时间 | 可以设置DTU在运行多久时间后，自动重启 |  |
| 串口无数据重启时间 | 设置后，启用的串口，如果超过这个时间没有接收到任何数据，DTU自动重启 |  |
| 网络无数据重启时间 | 设置后，启用的网络通道，如果超过这个时间没有接收到任何数据，DTU自动重启 |  |
| 远程控制命令 | 目标服务器是否能下发配置命令配置设备。(和串口执行命令相同) |  |
| NTP同步周期 | NTP网络时间同步功能，设备默认关闭，可以解决基站时间不对的问题 |  |
| 新连接清除缓存 | 网络通道断开重连的时候，删除本通道的发送缓存数据 |  |
| SIM卡切换 | 有的设备支持双卡，单待。目前只支持默认。插了外置卡，默认用外置卡 |  |
| 启用低功耗 | 支持低功耗的设备，再保持服务器连接(及时响应交互数据)，能名下降低功耗。 已S724C为例，不启用低功耗，大约5V 30ma.启用低功耗大约5V 4.5ma |  |
| TTS播报通道 | 设备支持耳机孔(可外接功放喇叭)和SPK口（直接驱动8欧2W喇叭），选择不同的接口播放 | 724支持 |
| 关闭LED | 关闭LED能减少更多的能耗 |  |
| 可控电源模式 | 设备启动电源输出方式 |  |
| 开启IPV6 | 是否支持IPV6,目前只有 | 780支持 |
| RNDIS | 是否支持RNDIS拨号上网 | 724支持 |

# 二、APN参数

APN ，对于普通卡来讲，能连上我们平台的，基本都不需要配置APN。如果您的卡是专网卡需要配置APN参数，但是专网卡默认是连不上这个配置平台的，所以需要用一个普通卡，先更新参数，保存到设备，保存参数后，再使用实际的卡。或者用串口工具配置。

|  |  |
|-|-|
| 参数项目 | 参数含义 |
| 加密方式 | 根据实际情况选择 |
| 名称 | SIM卡APN名称，如果有需要填写正确 |
| 用户名 | SIM卡APN用户名，如果有需要填写正确 |
| 密码 | SIM卡APN密码，如果有需要填写正确 |
| 类型 | 系统默认和公网卡是一个意思。 |

# 三、串口参数

|  |  |
|-|-|
| 参数项目 | 参数说明 |
| 是否启动 | 启动表示打开串口，如果关闭，串口不能通信 |
| 波特率 | 串口波特率1200\~921600 |
| 数据位 | Air724系列只支持8位数据，不支持7位数据 Air780支持7位和8位数据 |
| 校验位 | 无校验，奇、偶校验 |
| 停止位 | 1、2位 |
| 打包超时时间 | 串口数据分包超时时间，当2个字节的数据间隔大于这个时间后，将发送给服务器 |
| EN翻转时间 | EN影响RS485数据发送和接收。一般情况下用默认值调试，Air724 如果有乱码一般设置成1；Air780如果有乱码设置5000\~20000试试 |

# 四、自动轮询

自动轮询，是提前在DTU内保存对外轮询的命令，当设置命令后，DTU就会按照间隔自动发送数据给串口。当收到串口数据后，在自动发送到服务器，这样可以减少服务器的轮询流量，适合一个设备一条命令，或者返回的数据能区分长度和设备的场景。如果有很多设备，并且无法区分返回数据归属的情况下，一般用任务去实现，可以自由解析处理数据。

|  |  |
|-|-|
| 参数项目 | 参数说明 |
| 是否启动 | 启动后，就会发送对应的数据 |
| 轮询应答超时时间 | 当发送一条命令后，等待这个时间发送下一跳轮询命令。两条命令之间间隔 |
| 轮询周期时间 | 当设置的全部命令轮询完成后，等待这个时间后，继续从第一条命令开始轮询 |
| 轮询命令格式 | 轮询的数据格式，可以是字符串或者Hex16进制。DTU会根据类型转数据 |
| 轮询命令 | 需要发送的命令当设置字符串的时候，轮询原封不动的发送出去，设置Hex 16进制格式的时候，比如要设置0xaa 0x01 0xbb ，设置格式为 aa 01 bb或者aa01bb 大部分场景的命令是modbus命令 |

# 五、网络通道参数

网络通道参数，每个通道表示一个连接，有多少个通道就最多可以连接多少个服务器。设置DTU连接服务器的通信协议和对应的参数，分别支持TCP、UDP、MQTT、Websocket,阿里云IOT，腾讯IOT，华为IOT，移动物联网平台，电信IOT，涂鸦云，ThingsCloud云等协议连接。不同的模块对协议支持不一样，以具体设备功能为准。

## 5.1、TCP/UDP 参数

|  |  |  |
|-|-|-|
| 参数项目 | 参数说明 | 备注 |
| 是否启动 | 是否启动本通道 |  |
| 网络通信协议 | TCP/UDP |  |
| 绑定通信串口 | 本网络通道绑定串口硬件 |  |
| 心跳包包开关 | 作用:周期给服务器发数据，防止基站关闭设备网络；周期给服务器通讯，告诉服务器设备还正常在线。服务器通过是否收到心跳来判断设备是否网络正常。 原理:在没有任何网络数据交互的时候，开始倒计时。到达时间后，发送数据到服务器。服务器可以应答，可以不应答。 注意:如果普通心跳数据会影响业务逻辑，可以用填写业务数据上报。或者自己的设备周期发业务数据给服务器，服务器应答。 支持变量。 |  |
| 心跳包数据 |  |  |
| 心跳包发送间隔时间 |  |  |
| 服务器地址 | 服务器地址，支持域名和IP |  |
| 服务器端口 | 服务器端口号 |  |
| 数据前置字段 数据后置字段 | 作用:在有效数据的前面或者后面加上固定的标记，对心跳、注册包以外的所有数据有效。在UDP协议上面区分设备特别有用 |  |
| 登录注册信息 | 作用:每次新连接服务器后的第一个数据包,用来识别那个设备连接了服务器通道。 |  |
| 支持IPV6 | 如果服务器是IPV6地址需要打开，同时打开基本参数的IPV6功能 | 780支持 |
| 支持SSL | 目前只支持无证书的单向认证加密 | 780支持 |

## 5.2、MQTT参数

|  |  |  |
|-|-|-|
| 参数项目 | 参数说明 | 备注 |
| 是否启动 | 本通道是否启动 |  |
| 网络通信协议 | MQTT |  |
| 绑定通信串口 | 本网络通道绑定串口硬件 |  |
| 心跳包发送间隔时间 | 作用:MQTT 协议内部需要的心跳，服务器通过心跳判断设备是否在线。 注意:心跳是协议层的，不表现在应用层，无有效数据收发。只需要配置间隔即可。MQTT协议要求的心跳时间，一般建议60\~120秒。 |  |
| 服务器地址 | 服务器地址，支持域名或者IP |  |
| 服务器端口 | 服务器端口 |  |
| 登录客户端ID | 如果不填写或者\${IMEI}默认使用模组的IMEI |  |
| 登录用户名 | MQTT服务器的用户名 |  |
| 登录密码 | MQTT服务器的密码 |  |
| 协议版本 | Air724系列可设置3.1和3.1.1。Air780/Y100P系列只能设置3.1.1，其他无效。 |  |
| 清除会话 |  |  |
| 持久消息 |  |  |
| 订阅QOS | 订阅topic的QOS 等级 |  |
| 发部QOS | 发部topic的QOS 等级 |  |
| 订阅topic | 支持变量,支持多topic。 特别说明1:如果想在一个分组里面给设备配置不同的topic名字，用来区分设备，可以使用${IMEI}变量。比如配置xxx/${IMEI},自动替换设备真实的IMEI。 特别说明2:Air724支持传送数据。Air780支持传送topic名字和数据。具体看Air780 多topic任务使用方法。 特别说明3:多topic情况下，远程命令接收默认用第一个topic。 |  |
| 发布topic | 支持变量,支持多topic。 特别说明1:如果想在一个分组里面给设备配置不同的topic名字，用来区分设备，可以使用${IMEI}变量。比如配置xxx/${IMEI},自动替换设备真实的IMEI。 特别说明2:实现多topic发布的方法。Air724 使用headingcode配置数据识别码。Air780使用任务的方法解析数据，具体看Air780 多topic任务使用方法。 特别说明3：Air780使用任务支持不提前配置，直接发送任意topic名字和数据具体看Air780 多topic任务使用方法。 特别说明3:多topic发布，远程命令和注册包默认用第一个发布topic. |  |
| 设置遗嘱 | 作用：当设备离线后，服务器检查到设备离线后，服务器会离线信息发送给订阅者。 一般不建议用，可以用业务心跳去判断设备是否在线更靠谱。 |  |
| 遗嘱QOS |  |  |
| 遗嘱持久消息 |  |  |
| 遗嘱topic |  |  |
| 遗嘱内容 |  |  |
| 登录注册信息 | 作用:每次新连接服务器后的第一个数据包,用来识别那个设备连接了服务器通道。 |  |
| 支持IPV6 | 如果服务器是IPV6地址需要打开，同时打开基本参数的IPV6功能 | 780支持 |
| 支持SSL | 目前只支持无证书的单向认证加密 | 780支持 |

## 5.3、HTTP参数

|  |  |  |
|-|-|-|
| 参数项目 | 参数说明 | 备注 |
| 是否启动 | 是否启动本通道 |  |
| 网络通信协议 | HTTP |  |
| 绑定通信串口 | 本网络通道绑定串口硬件 |  |
| 请求方法 | GET/POST |  |
| 服务器地址 | DTU需要连接的地址，支持域名和IP,支持非证书SSL加密 http://www.test.com https://www.test.com 如果要支持SSL,使用https开头即可 |  |
| 服务器端口 | 服务器端口号,根据实际填写，http一般是80,https一般是443 |  |
| 请求的Url | 请求的地址，例如/ 或者/xxx 或者/xxx?或者/xxx?aaa= GET协议url说明:get会把串口的数据拼接到url上面，串口数据需要符合HTTP的urlencode编码规则。设置服务器地址为http://www.test.com,服务器端口为80，Url为/test?aaa=。 DTU收到串口数据后，最终给服务器的请求是 http://www.yinerda.com:80/test?aaa=串口数据。 POST协议url说明:没有特殊的，串口的数据是在body里面，串口数据不需要提前编码。  Air780EPM V 2.0.4版本后，url支持变量比如配置成 /xxxIMEI=\${IMEI} |  |
| 等待超时时间 | 等待HTTP返回超时时间。设置的是DTU内部，HTTP超时时间。实际上DTU自己实现3次重传。3次重传都失败后会自动重启，最大响应时间是（设置时间+5)\*7秒 |  |
| 是否自定义头部 | head参数传入自定义请求头，如果自定义请求头中存在Host、Connection、Content-Length三个请求头，其他的需要自己添加。head可以自定义添加，每个变量以/0d/0a结尾,可以用来做身份识别比如: Content-Type=application/octet-stream/0d/0aIMEI=\${IMEI}/0d/0a |  |
| 返回数据过滤 | HTTP默认返回的数据有结果码，头部和有效数据。如果过滤了，就只返回有效数据。 |  |
| 支持IPV6 | 如果服务器是IPV6地址需要打开，同时打开基本参数的IPV6功能 | 780支持 |
| 支持SSL | 目前只支持无证书的单向认证加密，目前参数内部无效 只需要服务器地址用https开头即可。 | 780支持 |

## 5.4、WebSocket参数

|  |  |  |
|-|-|-|
| 参数项目 | 参数说明 | 备注 |
| 是否启动 | 是否启动本通道 |  |
| 网络通信协议 | HTTP |  |
| 绑定通信串口 | 本网络通道绑定串口硬件 |  |
| 服务器地址 | DTU需要连接的地址，支持域名和IP,支持非证书SSL加密 ws://www.test.com ws://59.110.19.10:29779 wss://www.test.com 如果要支持SSL,使用wss开头即可 |  |
| 支持IPV6 | 如果服务器是IPV6地址需要打开，同时打开基本参数的IPV6功能 | 780支持 |
| 是否自定义头部 | head可以自定义添加，每个变量已/0d/0a结尾,可以用来做身份识别比如: TEST=12345/0d/0aIMEI=\${IMEI}/0d/0a |  |
| 心跳包包开关 | 作用:周期给服务器发数据，防止基站关闭设备网络；周期给服务器通讯，告诉服务器设备还正常在线。服务器通过是否收到心跳来判断设备是否网络正常。 原理:在没有任何网络数据交互的时候，开始倒计时。到达时间后，发送数据到服务器。服务器可以应答，可以不应答。 注意:如果普通心跳数据会影响业务逻辑，可以用填写业务数据上报。或者自己的设备周期发业务数据给服务器，服务器应答。 支持变量。 |  |
| 心跳包数据 |  |  |
| 心跳包发送间隔时间 |  |  |
| 服务器地址 | 服务器地址，支持域名和IP |  |
| 服务器端口 | 服务器端口号 |  |
| 数据前置字段 数据后置字段 | 作用:在有效数据的前面或者后面加上固定的标记，对心跳、注册包以外的所有数据有效。在UDP协议上面区分设备特别有用 |  |
| 登录注册信息 | 作用:每次新连接服务器后的第一个数据包,用来识别那个设备连接了服务器通道。 |  |

## 5.5、新移动物联网平台

新移动物理网平台是说2024年新发布的物联网平台，老的已经关闭了。

目前只有Air780方案支持，目前只支持MQTT协议，Air724不支持。

本功能一般必须配合任务来使用，因为平台发送命令后，需要应答平台命令，否则设备虽然收到了平台下发的数据，但是平台会给业务服务器推送下发失败的结果，导致不正常。这个参考提供任务demo。

|  |  |  |
|-|-|-|
| 参数项目 | 参数说明 | 备注 |
| 是否启动 | 是否启动本通道 |  |
| 网络通信协议 | 移动物联网平台，不是onenet studio平台 |  |
| 绑定通信串口 | 本网络通道绑定串口硬件 |  |
| 连接类型 | 一型一密:设备使用模块的IMEI作为DeviceId（设备ID） 一机一密:设备在平台创建后，根据实际参数去填写 |  |
| 心跳间隔 |  |  |
| 服务器地址 | MQTT接入地址，国内一般是mqtts.heclouds.com |  |
| 服务器端口 | 1883 |  |
| 产品ID |  |  |
| access_key | 一型一密参数 |  |
| 设备ID | 一机一密参数 |  |
| 设备密匙 | 一机一密参数 |  |
| 消息QOS |  |  |
| 订阅topic | 支持变量,支持多topic。 特别说明1:如果想在一个分组里面给设备配置不同的topic名字，用来区分设备，可以使用${IMEI}变量。比如配置xxx/${IMEI},自动替换设备真实的IMEI。 特别说明2:多topic情况下，远程命令接收默认用第一个topic。 特别说明3:订阅topic支持+通配符。 |  |
| 发布topic | 支持变量,支持多topic。 特别说明1:如果想在一个分组里面给设备配置不同的topic名字，用来区分设备，可以使用${IMEI}变量。比如配置xxx/${IMEI},自动替换设备真实的IMEI。 特别说明2：Air780使用任务支持不提前配置，直接发送任意topic名字和数据具体看Air780 多topic任务使用方法。 特别说明3:多topic发布，远程命令和注册包默认用第一个发布topic. |  |
| 登录注册信息 | 作用:每次新连接服务器后的第一个数据包,用来识别那个设备连接了服务器通道。 |  |

## 5.6、老移动物联网平台(移动Onenet)

目前只有Air724方案支持，Air780不支持。

由于移动平台，关闭了老的移动物联网平台，多协议导致连接不上，此功能用不了。涉及到的是多协议接入TCP协议，多协议接入modbus协议，MQTT物联网套件协议。

## 5.7、电信AIOT

目前只有Air780方案支持，Air724不支持。

本功能一般必须配合任务来使用，因为平台发送命令后，需要应答平台命令，否则设备虽然收到了平台下发的数据，但是平台会给业务服务器推送下发失败的结果，导致不正常。这个参考提供任务demo。

|  |  |  |
|-|-|-|
| 参数项目 | 参数说明 | 备注 |
| 是否启动 | 是否启动本通道 |  |
| 网络通信协议 | 电信AIOT |  |
| 绑定通信串口 | 本网络通道绑定串口硬件 |  |
| 连接类型 | 一型一密:设备使用模块的IMEI作为DeviceId（设备ID） 一机一密:设备在平台创建后，根据实际参数去填写 |  |
| 心跳间隔 |  |  |
| 传送数据类型 | 目前DTU内部无效 |  |
| 服务器地址 | MQTT接入地址 |  |
| 服务器端口 | 1883 |  |
| 产品ID |  |  |
| 产品特征字符串 | 一型一密参数 |  |
| 设备ID | 一机一密参数 |  |
| 设备特征串认证 | 一机一密参数 |  |
| 清除会话 |  |  |
| 消息QOS |  |  |
| 订阅topic | 支持变量,支持多topic。 特别说明1:如果想在一个分组里面给设备配置不同的topic名字，用来区分设备，可以使用${IMEI}变量。比如配置xxx/${IMEI},自动替换设备真实的IMEI。 特别说明2:多topic情况下，远程命令接收默认用第一个topic。 特别说明3:订阅topic支持+通配符。 |  |
| 发布topic | 支持变量,支持多topic。 特别说明1:如果想在一个分组里面给设备配置不同的topic名字，用来区分设备，可以使用${IMEI}变量。比如配置xxx/${IMEI},自动替换设备真实的IMEI。 特别说明2：Air780使用任务支持不提前配置，直接发送任意topic名字和数据具体看Air780 多topic任务使用方法。 特别说明3:多topic发布，远程命令和注册包默认用第一个发布topic. |  |
| 登录注册信息 | 作用:每次新连接服务器后的第一个数据包,用来识别那个设备连接了服务器通道。 |  |

## 5.9、华为IOT

目前只有Air780方案支持，Air724不支持。

本功能一般必须配合任务来使用，因为平台发送命令后，需要应答平台命令，否则设备虽然收到了平台下发的数据，但是平台会给业务服务器推送下发失败的结果，导致不正常。这个参考提供任务demo。

华为的自动注册注意需要使用IAM账号信息，注意账号权限设置，同时设置DTU参数密码，防止泄露。

|  |  |  |
|-|-|-|
| 参数项目 | 参数说明 | 备注 |
| 是否启动 | 是否启动本通道 |  |
| 网络通信协议 | 华为IotDA |  |
| 绑定通信串口 | 本网络通道绑定串口硬件 |  |
| 连接类型 | 自动注册:设备使用模块的IMEI作为自动注册 一机一密:设备在平台创建后，根据实际参数去填写 |  |
| 心跳间隔 |  |  |
| 服务器地址 | MQTT接入地址 |  |
| 服务器端口 | 8883 |  |
| 实例ID | 自动注册参数 |  |
| 引用接入HTTPS地址 | 自动注册参数，实例的HTTPS应用接入地址，用来自动创建设备的 |  |
| 产品ID | 自动注册参数 |  |
| 空间资源APPID | 自动注册参数 |  |
| IAM服务器地址 | 自动注册参数 |  |
| IAM用户名 | 自动注册参数 |  |
| IAM账号名 | 自动注册参数 |  |
| IAM密码 | 自动注册参数 |  |
| 项目 | 自动注册参数 |  |
| 项目ID | 自动注册参数 |  |
| clientId | 一机一密参数 |  |
| username | 一机一密参数 |  |
| password | 一机一密参数 |  |
| 清除会话 |  |  |
| 消息QOS |  |  |
| 订阅topic | 支持变量,支持多topic。 特别说明1:如果想在一个分组里面给设备配置不同的topic名字，用来区分设备，可以使用${IMEI}变量。比如配置xxx/${IMEI},自动替换设备真实的IMEI。 特别说明2:多topic情况下，远程命令接收默认用第一个topic。 特别说明3:订阅topic支持+通配符。 |  |
| 发布topic | 支持变量,支持多topic。 特别说明1:如果想在一个分组里面给设备配置不同的topic名字，用来区分设备，可以使用${IMEI}变量。比如配置xxx/${IMEI},自动替换设备真实的IMEI。 特别说明2：Air780使用任务支持不提前配置，直接发送任意topic名字和数据具体看Air780 多topic任务使用方法。 特别说明3:多topic发布，远程命令和注册包默认用第一个发布topic. |  |
| 登录注册信息 | 作用:每次新连接服务器后的第一个数据包,用来识别那个设备连接了服务器通道。 |  |

## 5.10、新腾讯IOT Explorer

目前只有Air780方案支持，Air724不支持。串口数据一般使用透传功能，如果使用物模型，需要按物模型组装数据格式，然后上传。或者使用任务的方式在任务里面解析数据上传物模型数据。

|  |  |  |
|-|-|-|
| 参数项目 | 参数说明 | 备注 |
| 是否启动 | 是否启动本通道 |  |
| 网络通信协议 | 新腾讯IOT Explorer |  |
| 绑定通信串口 | 本网络通道绑定串口硬件 |  |
| 连接类型 | 一型一密:设备ID必须用设备IMEI 一机一密:设备在平台创建后，根据实际参数去填写 |  |
| 心跳间隔 |  |  |
| Region | 目前只有ap-guangzhou |  |
| ProductID |  |  |
| ProductSecret | 一型一密参数 |  |
| DeviceName | 一机一密参数 |  |
| DeviceSecret | 一机一密参数 |  |
| 清除会话 |  |  |
| 消息QOS |  |  |
| 订阅topic | 支持变量,支持多topic。 特别说明1:如果想在一个分组里面给设备配置不同的topic名字，用来区分设备，可以使用${IMEI}变量。比如配置xxx/${IMEI},自动替换设备真实的IMEI。 特别说明2:多topic情况下，远程命令接收默认用第一个topic。 特别说明3:订阅topic支持+通配符。 |  |
| 发布topic | 支持变量,支持多topic。 特别说明1:如果想在一个分组里面给设备配置不同的topic名字，用来区分设备，可以使用${IMEI}变量。比如配置xxx/${IMEI},自动替换设备真实的IMEI。 特别说明2：Air780使用任务支持不提前配置，直接发送任意topic名字和数据具体看Air780 多topic任务使用方法。 特别说明3:多topic发布，远程命令和注册包默认用第一个发布topic. |  |
| 登录注册信息 | 作用:每次新连接服务器后的第一个数据包,用来识别那个设备连接了服务器通道。 |  |

## 5.11、老腾讯IOT Explorer（腾讯IOT）

只有Air724支持，2024年3月18之前开通的腾讯IOT可以用。之后开的的IOT也连接不上，需要换Air780方案。

|  |  |  |
|-|-|-|
| 参数项目 | 参数说明 | 备注 |
| 是否启动 | 是否启动本通道 |  |
| 网络通信协议 | 老腾讯IOT Explorer |  |
| 绑定通信串口 | 本网络通道绑定串口硬件 |  |
| 心跳间隔 |  |  |
| Region | 目前只有ap-guangzhou |  |
| ProductID |  |  |
| ProductSecret |  |  |
| DeviceName | 默认使用\${IMEI}自动注册设备 |  |
| 清除会话 |  |  |
| 消息QOS |  |  |
| 订阅topic | 支持变量,支持多topic。 特别说明1:如果想在一个分组里面给设备配置不同的topic名字，用来区分设备，可以使用${IMEI}变量。比如配置xxx/${IMEI},自动替换设备真实的IMEI。 特别说明2:多topic情况下，远程命令接收默认用第一个topic。 特别说明3:订阅topic支持+通配符。 |  |
| 发布topic | 支持变量,支持多topic。 特别说明1:如果想在一个分组里面给设备配置不同的topic名字，用来区分设备，可以使用${IMEI}变量。比如配置xxx/${IMEI},自动替换设备真实的IMEI。 特别说明2：Air780使用任务支持不提前配置，直接发送任意topic名字和数据具体看Air780 多topic任务使用方法。 特别说明3:多topic发布，远程命令和注册包默认用第一个发布topic. |  |
| 登录注册信息 | 作用:每次新连接服务器后的第一个数据包,用来识别那个设备连接了服务器通道。 |  |

## 5.12、新阿里IOT

**新老阿里云主要是在公版实例上面有区别，新老阿里云的区别是老阿里没有实例ID和独立的MQTT接入地址，如果有（新）Air780支持，如果没有（老）Air724支持。**

**阿里云在企业版都有实例ID和MQTT接入地址，在使用上面没有区别，Air780和Air724都支持。**

串口数据一般使用透传功能，如果使用物模型，需要按物模型组装数据格式，然后上传。或者使用任务的方式在任务里面解析数据上传物模型数据。

|  |  |  |
|-|-|-|
| 参数项目 | 参数说明 | 备注 |
| 是否启动 | 是否启动本通道 |  |
| 网络通信协议 | 新阿里IOT |  |
| 绑定通信串口 | 本网络通道绑定串口硬件 |  |
| 连接类型 | 一型一密:设备名字必须用设备IMEI 一机一密:设备在平台创建后，根据实际参数去填写 |  |
| 心跳间隔 |  |  |
| 服务器地址 | MQTT接入地址 |  |
| 服务器端口 | MQTT接入端口,1883 |  |
| 实例ID |  |  |
| ProductKey |  |  |
| ProductSecret | 一型一密参数 |  |
| DeviceSecret | 一机一密参数 |  |
| DeviceName | 一机一密参数 |  |
| 清除会话 |  |  |
| 消息QOS |  |  |
| 订阅topic | 支持变量,支持多topic。 特别说明1:如果想在一个分组里面给设备配置不同的topic名字，用来区分设备，可以使用${IMEI}变量。比如配置xxx/${IMEI},自动替换设备真实的IMEI。 特别说明2:多topic情况下，远程命令接收默认用第一个topic。 特别说明3:订阅topic支持+通配符。 |  |
| 发布topic | 支持变量,支持多topic。 特别说明1:如果想在一个分组里面给设备配置不同的topic名字，用来区分设备，可以使用${IMEI}变量。比如配置xxx/${IMEI},自动替换设备真实的IMEI。 特别说明2：Air780使用任务支持不提前配置，直接发送任意topic名字和数据具体看Air780 多topic任务使用方法。 特别说明3:多topic发布，远程命令和注册包默认用第一个发布topic. |  |
| 登录注册信息 | 作用:每次新连接服务器后的第一个数据包,用来识别那个设备连接了服务器通道。 |  |

## 5.13、老阿里IOT（阿里云）

**新老阿里云主要是在公版实例上面有区别，新老阿里云的区别是老阿里没有实例ID和独立的MQTT接入地址，如果有（新）Air780支持，如果没有（老）Air724支持。**

**阿里云在企业版都有实例ID和MQTT接入地址，在使用上面没有区别，Air780和Air724都支持**

新老阿里云的区别是，老阿里云没有实例ID和独立的MQTT接入地址。如果有(企业版)新老都可以用。

串口数据一般使用透传功能，如果使用物模型，需要按物模型组装数据格式，然后上传。或者使用任务的方式在任务里面解析数据上传物模型数据。

|  |  |  |
|-|-|-|
| 参数项目 | 参数说明 | 备注 |
| 是否启动 | 是否启动本通道 |  |
| 网络通信协议 | 阿里IOT |  |
| 绑定通信串口 | 本网络通道绑定串口硬件 |  |
| 链接类型 | 一型一密:设备名字必须用设备IMEI 一机一密:设备在平台创建后，根据实际参数去填写 |  |
| 心跳间隔 |  |  |
| 地域代码(RegionID | cn-shanghai，和账号注册区域有关系，需要选择正确 |  |
| ProductKey |  |  |
| ProductSecret | 一型一密参数 |  |
| DeviceSecret | 一机一密参数 |  |
| Devicename | 一机一密参数 |  |
| 清除会话 |  |  |
| 消息QOS |  |  |
| 订阅topic | 支持变量,支持多topic。 特别说明1:如果想在一个分组里面给设备配置不同的topic名字，用来区分设备，可以使用${IMEI}变量。比如配置xxx/${IMEI},自动替换设备真实的IMEI。 特别说明2:多topic情况下，远程命令接收默认用第一个topic。 特别说明3:订阅topic支持+通配符。 |  |
| 发布topic | 支持变量,支持多topic。 特别说明1:如果想在一个分组里面给设备配置不同的topic名字，用来区分设备，可以使用${IMEI}变量。比如配置xxx/${IMEI},自动替换设备真实的IMEI。 特别说明2：Air780使用任务支持不提前配置，直接发送任意topic名字和数据具体看Air780 多topic任务使用方法。 特别说明3:多topic发布，远程命令和注册包默认用第一个发布topic. |  |
| 登录注册信息 | 作用:每次新连接服务器后的第一个数据包,用来识别那个设备连接了服务器通道。 |  |
| 阿里MQTT域名 | 企业版MQTT独立的接入地址 |  |

## 5.14、涂鸦云

涂鸦云的第三方生态接入协议。

目前只有Air780方案支持，Air724不支持。

本功能一般必须配合任务来使用，因为平台发送命令后，需要应答平台命令，否则设备虽然收到了平台下发的数据，但是平台会给业务服务器推送下发失败的结果，导致不正常。这个参考提供任务demo。

|  |  |  |
|-|-|-|
| 参数项目 | 参数说明 | 备注 |
| 是否启动 | 是否启动本通道 |  |
| 网络通信协议 | 涂鸦云 |  |
| 绑定通信串口 | 本网络通道绑定串口硬件 |  |
| 连接类型 | 一机一密:设备在平台创建后，根据实际参数去填写 目前平台只支持一机一密 |  |
| 心跳间隔 |  |  |
| 传送数据类型 | 目前DTU内部无效 |  |
| 服务器地址 | MQTT接入地址，国内是m1.tuyacn.com |  |
| 服务器端口 | MQTT接入端口,8883 |  |
| 实例ID |  |  |
| DeviceID |  |  |
| DeviceSecret |  |  |
| 清除会话 |  |  |
| 消息QOS |  |  |
| 订阅topic | 支持变量,支持多topic。 特别说明1:如果想在一个分组里面给设备配置不同的topic名字，用来区分设备，可以使用${IMEI}变量。比如配置xxx/${IMEI},自动替换设备真实的IMEI。 特别说明2:多topic情况下，远程命令接收默认用第一个topic。 特别说明3:订阅topic支持+通配符。 |  |
| 发布topic | 支持变量,支持多topic。 特别说明1:如果想在一个分组里面给设备配置不同的topic名字，用来区分设备，可以使用${IMEI}变量。比如配置xxx/${IMEI},自动替换设备真实的IMEI。 特别说明2：Air780使用任务支持不提前配置，直接发送任意topic名字和数据具体看Air780 多topic任务使用方法。 特别说明3:多topic发布，远程命令和注册包默认用第一个发布topic. |  |
| 登录注册信息 | 作用:每次新连接服务器后的第一个数据包,用来识别那个设备连接了服务器通道。 |  |

## 5.15、ThingsCloud

目前只有Air780方案支持，Air724不支持。

本功能一般必须配合任务来使用，因为平台发送命令后，需要应答平台命令，否则设备虽然收到了平台下发的数据，但是平台会给业务服务器推送下发失败的结果，导致不正常。这个参考提供任务demo。

|  |  |  |
|-|-|-|
| 参数项目 | 参数说明 | 备注 |
| 是否启动 | 是否启动本通道 |  |
| 网络通信协议 | ThingsCloud |  |
| 绑定通信串口 | 本网络通道绑定串口硬件 |  |
| 连接类型 | 自动注册，使用模块IMEI自动注册 目前平台只支持一机一密 |  |
| 心跳间隔 |  |  |
| 服务器地址 | MQTT接入地址 |  |
| 服务器端口 | MQTT接入端口,1883 |  |
| API接入点 | API接入地址，用来自动创建设备 |  |
| ProjectKey |  |  |
| TypeKey |  |  |
| 订阅topic | 支持变量,支持多topic。 特别说明1:如果想在一个分组里面给设备配置不同的topic名字，用来区分设备，可以使用${IMEI}变量。比如配置xxx/${IMEI},自动替换设备真实的IMEI。 特别说明2:多topic情况下，远程命令接收默认用第一个topic。 特别说明3:订阅topic支持+通配符。 |  |
| 发布topic | 支持变量,支持多topic。 特别说明1:如果想在一个分组里面给设备配置不同的topic名字，用来区分设备，可以使用${IMEI}变量。比如配置xxx/${IMEI},自动替换设备真实的IMEI。 特别说明2：Air780使用任务支持不提前配置，直接发送任意topic名字和数据具体看Air780 多topic任务使用方法。 特别说明3:多topic发布，远程命令和注册包默认用第一个发布topic. |  |
| 登录注册信息 | 作用:每次新连接服务器后的第一个数据包,用来识别那个设备连接了服务器通道。 |  |

# 六、数据流模板

数据流模板，用来对数据做转换使用的一个片段代码。比如json转hex，或者数据条件过滤等。可以参考数据流模板demo。

注意1:Air780使用的lua 5.3 ，Air724使用的是Lua 5.1，并且API函数不兼容，用错了会出错,导致设备崩溃。

注意2:调试的时候需要用USB_luatool工具打印日志来调试。

注意3:在你们确认功能完成之前，不要用在生产环境，单独有设备测试稳定后再放到生产环境。

|  |  |  |
|-|-|-|
| 参数项目 | 参数说明 | 备注 |
| 是否启动 | 是否启动，注意启动后数据 |  |
| 发送数据模版 | 串口收到数据，经过本模板处理后，传给服务器 |  |
| 接收数据模版 | 服务器收到数据，经过本模板处理后，传给串口 |  |

# 七、任务

任务，一个一直循环执行，周期执行的代码，可以用于自动采集，数据转换，超低功耗，自定义逻辑，TTS播报，GPS采集上报等，可以参考任务demo。

注意1:Air780使用的lua 5.3 ，Air724使用的是Lua 5.1，并且API函数不兼容，用错了会出错,导致设备崩溃。

注意2:调试的时候需要用USB_luatool工具打印日志来调试。

注意3:在你们确认功能完成之前，不要用在生产环境，单独有设备测试稳定后再放到生产环境。

# 八、GPIO

Air724系列支持GPIO，Air780 不支持GPIO，Air780使用任务实现GPIO功能。AIr724也能使用任务GPIO功能，并且更灵活强大，逻辑自定义。

建议后续都用任务实现GPIO的检测，上报和本地联动控制。

# 九、定位

定位分基站定位，WIFI 定位，GPS定位和。

基站定位偏差大500\~几公里，可以在室内定位。

基站定位周期最小60秒。



WIFI定位在50\~200米，可以室内定位，附近必须要附近有WIFI。

WIFI和基站定位同时存在的时候，以WIFI定位优先，如果WIFI定位失败会用基站定位数据定位。

WIFI和基站信息都在定位服务器数据库内才能定位，否则也定不了位。

WIFI和基站定位必须在大陆才能有用。



GPS定位准去，必须在室外空旷的地方定位。

GPS定位周期最小60秒。

定位信息可以自动上报，但是可能和串口数据同时到达服务器，导致冲突。这一般可以用任务处理做成。任何可以灵活的组装和拼接各种数据格式。

|  |  |  |
|-|-|-|
| 参数项目 | 参数说明 | 备注 |
| 是否启动 | 是否启动，注意启动后数据 |  |
| 更新时间 | 定位间隔时间。周期定位，基站/WIFI定位需要消耗流量，大约1K一次。 |  |
| 主动上报内容格式 | 定位成功后，到达上报时间后，上报数据，可以使用变量。 不支持原始数据。 |  |
| 主动上报通道 | 定位成功后，到达上报时间后，上报数据 |  |

# 十、短信转发

Air780支持，Air724不支持。

使用手机卡，可以接受短信，并且把接收到的短信转发到目标服务器。

需要提前打开这个功能，串口才能获取和发送短信，如果不需要上报就不上报。

开启后，任务里面可以调用发送短信的API。

|  |  |  |
|-|-|-|
| 参数项目 | 参数说明 | 备注 |
| 是否启动 | 是否启动 |  |
| 是否过滤 | 是否过滤接收的短信号码，如果过滤了只有被允许的号码才能转发。 |  |
| 允许号码 |  |  |
| 短信上报格式 | 固定格式和自定义格式，指定一个可以使用变量 |  |
| 短信上报内容 | 字符串格式支持变量 Air780EPM方案固件版本大于等于V2.0.4版本，这个地方也支持标准的变量比如${IMEI},${ICCID} |  |
| 上报通道 |  |  |
| 内部缓存 | 只有打开了缓存，任务可以获取并处理收到的短信。串口也可以获取短信 |  |

# 十一、加密证书

V1.1.12版本及其以上固件支持，TCP，HTTPS，普通MQTT协议支持。

使用的时候，在网络他通道产参数里面先设置支持SSL参数为有证书加密。

|  |  |  |
|-|-|-|
| 参数项目 | 参数说明 | 备注 |
| 是否启动 | 如果启用了，参数需要配置正确 |  |
| 加密类型 | 目前只支持TLS1.2 |  |
| 校验机制 | 不校验，可选校验，强制校验 这些只有在普通MQTT协议支持，TCP和HTTP不支持 |  |
| 服务器证书数据 | 服务器ca证书数据, 可选, 一般不需要，一般是server_ca.crt文件 |  |
| 客户端证书数据 | 客户端ca证书数据, 可选, 一般不需要, 双向认证才需要 client.crt文件 |  |
| 客户端私钥加密数据 | 客户端私钥加密数据, 可选, 一般不需要, 双向认证才需要 client.key文件 |  |
| 客户端私钥口令数据 | 客户端私钥口令数据, 可选, 一般不需要, 双向认证才需要 clinet.password文件 |  |

# 十二、固件升级

V1.1.12版本及其以上固件支持。固件升级是DTU下载设备需要的升级固件，然后分包传送设备，不是说的DTU自身的固件升级。

使用的时候需要串口配合upcheck和upin命令，具体参看:[https://yinerda.yuque.com/yt1fh6/4gdtu/py7g1e2x5rh7tma1#](https://yinerda.yuque.com/yt1fh6/4gdtu/py7g1e2x5rh7tma1)

获取固件文件有2种方式，一种是自建服务器，支持断点续传。只需要配置文件下载的url即可。

一种是使用银尔达文件服务器，上传文件服务器后，会得到一个文件ID，把文件ID配置进去即可。

|  |  |  |
|-|-|-|
| 参数项目 | 参数说明 | 备注 |
| 是否启动 | 如果启用了，参数需要配置正确 |  |
| 文件版本号 | 自定义的版本号，MCU主板可以发upcheck命令 获取到返回 一般用于与自身固件配合决定是否升级。 |  |
| 文件获取方式 | 文件ID: 银尔达文件服务器 下载url :自建服务器使用 |  |
| 文件下载路径 | 如果获取方式是文件ID: 填写银尔达文件管理里面的文件ID 如果获取方式是下载url：填写文件的下载路径 |  |
| 绑定通讯串口 | 使用那个串口下载固件 |  |
| 每包长度 | 每一个下载包的长度。防止设备串口数据超过接受能力 内存和串口传送，都会决定文件可能一次性下载，必须分包后慢慢下载 |  |
| 每包间隔 | 一个包与一个包之间的最小间隔。防止设备串口数据超过接受能力 实际上由于网络延迟理论是大于这个时间的 |  |
| 数据识别码 | 无:表示数据纯透传，不做任何标记 数据识别码：每一个包有固定格式，"UPFILE:"+"文件总长度:"+"当前长度:"+"本报包长度:"+数据。方便用户解析数据 前4个字段格式都是字符串的使用:间隔 |  |