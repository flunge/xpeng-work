# 【断点续接文档】仿真部 team memory 工程

> 最后核实：2026-07-08（由灵犀在新会话中重新核对工作区真实状态后重写）
> 用途：会话重启后直接读本文件即可接续，不依赖人肉转贴。

## 环境
- 工作目录 `/workspace/team`，通过 `lark-cli`（身份 `--as user`）操作飞书
- 项目→飞书 token 映射：`memory/_feishu_map.json`（projects/people/teams/... 分区）

## 关键文档 token
| 文档 | token |
|---|---|
| Q3作战表（周目标&进展，6列5主线） | `SBUYwm8Lri9aJ6kmexFcBAuGnlh` |
| 本周周报 | `RIEvd6fKVo3NzoxkInTcK4IznUe` |
| 双周报（给刘先明，架构图+4时间线图） | `A42GdtJGMopqqBxhF2McE7v7n8c` |
| 溯源总索引 | `SsWCdQbVZohGHFxhE3RcCmJ2nSb` |
| Q3 OKR主文档 | `M8iUwlfAhi190Sk2xIwcHLAWn8f` |
| 8月日报 / 9月日报 | `Pdhud83wwo1d4KxnS9dcma1nn1g` / `KtzpdQdcxotyGZxdmpgcJpnInef` |
| 场景编辑 ledger | `NAqbdVbuxoDn7jxjrEzcyHmynDe` |
| 溯源索引（feishu_map 内 index_doc） | `UwiEdTJJ2oRGokxtkE2cJXjwnyb` |

## 已完成
1. Q2全时间线（W13-W27）精读回填各人 profile
2. H1绩效表（只看当期+职级校准，含周蔚旭 B-）
3. 溯源索引重建（项目维度）
4. 作战表 7月 W28-31 改 6列（①车型泛化②闭环+HIL③生产链路④Agent⑤算法预研）、周目标 task化、每日进展按子项目 @人、⑤算法预研 5天合并 cell
5. 双周报 A42：架构图（韩阿东P5/张友健P7 绿色NEW）+ 4项目时间线图
6. 周报场景编辑章节+图
7. 【更新记忆】命令规则写入 `team/CLAUDE.md`
8. **日报同步（W27-W28）已完成 14 个项目**（见 `.sync_done.txt`）：
   车型泛化 / RC路线 / HIL链路部署 / 慢速模式 / 极速模式 / 复现率Agent /
   TopDiff-Agent / Prompt-Agent / CLIP-IQA / Fixer优化 / RC路线SIL验证 /
   闭环场景集推进 / **场景泛化** / WM-内部探索

## 未完成（下会话接着做）
**唯一剩：场景编辑 ledger 日报同步。**

⚠️ **不能直接跑 `python3 scripts/sync_daily.py 场景编辑`** —— `sync_daily.py` 的 `DATA`
字典里**没有 `场景编辑` 这个 key**，脚本会 `continue` 跳过、静默不写。

正确步骤：
1. 从**作战表 `SBUYwm8L`（或原始日报）**取场景编辑 6/29–7/3 逐日进展原文
   （摘要方向：未渲染出编辑车修复 → 编辑数据外挂解耦 → webviz/metric 全链路打通 → 3dgs坐标系仍偏移）
2. 在 `scripts/sync_daily.py` 的 `DATA` 里新增 `"场景编辑":("场景编辑","""<逐日 bullet>""")`
   （`tok("场景编辑")` 会命中 token `NAqbdVbuxoDn7jxjrEzcyHmynDe`）
3. 运行 `cd /workspace/team && python3 scripts/sync_daily.py 场景编辑`
4. 确认 `.sync_done.txt` 追加了 `场景编辑`

⚠️ 禁止凭箭头摘要编造逐日 bullet，必须取原文。

## 收尾
- 更新 `memory/.last-memory-update` = 同步完成日（当前该文件不存在，需新建）
- 清理临时/中间文件：`.sync_tmp.md`、`.W29v2/.W30v2/.W31v2.xml`、`.W29v3/.W30v3/.W31v3.xml`、
  `.q2meetings.txt`、`.sync_done.txt`（如确认同步全部收官）等
